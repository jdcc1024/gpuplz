class StockData:

    def __init__(self, status, name, url, quick_url, notes):
        self.status = status
        self.name = name
        self.url = url
        self.quick_url = quick_url
        self.notes = notes
